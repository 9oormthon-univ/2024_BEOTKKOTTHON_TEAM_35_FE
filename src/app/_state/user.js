import { atom } from "recoil";

export const userEmail = atom({
  key: 'userEmail',
  default: false
});

export const userPassword1 = atom({
  key: 'userPassword1',
  default: false
});

export const userPassword2 = atom({
  key: 'userPassword2',
  default: false
});